pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
//        mavenLocal()
        // add Microblink maven repository to repositories list
        maven { url 'https://maven.microblink.com' }
    }
}
rootProject.name = "CaptureSample"
include ':app'
include ':app-direct-api'
include ':lib-common'
